/* ============================================================
   FRAMATICS — site.js
   Global JS for the Webflow site. Served via Cloudflare Pages.
   Referenced in Webflow Project Settings → Custom Code → footer
   GSAP is loaded separately via the registered GSAP Suite Loader.
   This file handles: section init coordination, global helpers.
   ============================================================ */

(function () {
  'use strict';

  /* Section modules register themselves here via window.Webflow.push()
     or by listening for DOMContentLoaded. This file bootstraps the
     coordination layer so each module can be added independently. */

})();
